import type React from "react";

export const BASE_CONTAINER_STYLE: React.CSSProperties = {
    position: "relative",
}

export const BASE_EL_STYLE: React.CSSProperties = {
    position: "absolute",
}